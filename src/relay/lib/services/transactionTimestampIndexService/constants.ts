// SPDX-License-Identifier: Apache-2.0

/** Key namespace for hash to consensus timestamp entries, shared by the local and Redis implementations. */
export const TIMESTAMP_INDEX_KEY_PREFIX = 'txtimestamp:hash:';
